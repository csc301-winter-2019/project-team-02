extends layout.pug
block content
	#sidebar
		h1 This is a sidebar
	#map

	script(type='text/javascript').
		var map = L.map('map').setView([#{lat},#{lng}], 13);
		var dumbpoints = !{JSON.stringify(points)};
		//console.log(dumbpoints);
	script(src='/javascripts/map.js')
